# Render Blueprint for ResumeForge
services:
  - type: web
    name: resumeforge
    env: node
    plan: free
    region: oregon
    buildCommand: npm install && npm run reset-db
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
        sync: false
      - key: PORT
        value: 3000
      # GitHub token is optional - users should provide their own to avoid rate limits
      # - key: GITHUB_TOKEN
      #   value: your_token_here
    healthCheckPath: /
    disk:
      name: resumeforge-data
      mountPath: /app/data
      sizeGB: 1
